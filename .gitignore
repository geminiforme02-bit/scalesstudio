# Dependencies (dev-only: used by screenshot.mjs)
node_modules/

# Local QA artifacts
temporary screenshots/
_*.mjs
_*.png

# Source video (not used by the site — only the extracted frames/ are)
*.mp4

# Source flyer screenshot (brand assets are generated from it)
Screenshot *.png

# Build-instruction notes (machine-specific)
CLAUDE (1).md
SKILL (2).md

# OS / editor
.DS_Store
Thumbs.db
